<!DOCTYPE html>
<html>
  <head>
    <title><%= document.title %></title>
  </head>
  <body>
    <img src='<%= renderer.tmpdir.join("images", File.basename(document.cover)) %>' width='100%' />
  </body>
</html>